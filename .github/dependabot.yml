version: 2
updates:
  # The package itself has no dependencies; the actions used by the
  # workflows above do get updates worth tracking.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
